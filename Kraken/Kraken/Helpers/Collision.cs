using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Kraken.Helpers
{ 
    public enum VerticalDirection
    {
        Below = 1,
        Up = -1,
        Equal = 0
    }

    public enum HorizontalDirection
    {
        Left = -1,
        Right = 1,
        Equal = 0
    }
}
